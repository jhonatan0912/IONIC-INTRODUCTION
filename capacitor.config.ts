import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.componentes',
  appName: 'componentes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
